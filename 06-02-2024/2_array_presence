#include <stdio.h>

int main(void){
    // initialising an array of 10 numbers
    int arr[10];
    // initialising the number we need to check 
    int num;
    
    // taking the input
    for(int i=0; i<10; i++){
        scanf("%d", &arr[i]);
    }
    
    // input of the number to check
    scanf("%d", &num);
    
    // now we'll see if the number is present or not
    for(int i=0; i<10; i++){
        if(arr[i]==num){
            printf("1");
            return 0;
        }
    }
    printf("0");
    
    


}
